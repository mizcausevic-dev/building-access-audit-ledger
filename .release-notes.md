# v0.1-shipped

- initial public ship of `building-access-audit-ledger`
- C# / ASP.NET proptech operator surface for instrument changes, firmware/calibration gaps, and audit-safe attestation posture
- live routes: `/`, `/access-lane`, `/event-log`, `/control-posture`, `/verification`, `/docs`
- synthetic sample data only
- Pages deploy with `accesslog.kineticgain.com`
